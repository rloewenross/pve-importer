server {
    listen 80;
    listen 443 ssl;
    ssl_certificate {{ pve_importer_https_cert_dest_path }};
    ssl_certificate_key {{ pve_importer_https_key_dest_path }};

    server_name {{ pve_importer_web_url }};
    root {{ pve_importer_install_path }}/public;

    location / {
        try_files $uri /index.php$is_args$args;
    }

    location ~ ^/index\.php(/|$) {
        fastcgi_pass unix:{{ pve_importer_php_socket }};
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include /etc/nginx/fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;
        internal;
    }

    location ~ \.php$ {
        return 404;
    }
}
